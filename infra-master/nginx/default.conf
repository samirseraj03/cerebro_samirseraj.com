# Rutas de dirección para cada proyecto
# Proxy inverso Nginx para enrutar tráfico a los contenedores Docker

# Frontend - Servidor por defecto y samirseraj.com
server {
    listen 80 default_server;
    server_name samirseraj.com localhost;

    resolver 127.0.0.11 valid=10s;

    location / {
        set $upstream_frontend http://web-frontend:80;
        proxy_pass $upstream_frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Django Backend
server {
    listen 80;
    server_name api.samirseraj.com api.localhost;

    resolver 127.0.0.11 valid=10s;

    location / {
        set $upstream_django http://django-backend:8000;
        proxy_pass $upstream_django;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# FastAPI (puerto correcto: 8000, no 8080)
server {
    listen 80;
    server_name fastapi.samirseraj.com fastapi.localhost;

    resolver 127.0.0.11 valid=10s;

    location / {
        set $upstream_fastapi http://fastapi-service:8000;
        proxy_pass $upstream_fastapi;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Laravel
server {
    listen 80;
    server_name laravel.samirseraj.com laravel.localhost;

    resolver 127.0.0.11 valid=10s;

    location / {
        set $upstream_laravel http://laravel-app:80;
        proxy_pass $upstream_laravel;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}